# Kineto 康复动作视频拍摄规范

> 本文档基于引擎实际管线编写，所有参数均可溯源至代码。
> 教练拍摄时遵循本规范可显著提升 3D 姿态还原质量；算法侧优化建议见末章。

---

## 1. 管线能力边界（拍摄前必读）

Kineto 的姿态还原链路：

```
视频帧 → YOLOv8 人体检测(bbox) → HMR2(SMPL) 3D 回归 → YOLOv8n-pose COCO-17 检测
→ 2D 锚定(射线球交) → 骨长/时序修正 → 交付 joints_3d + smpl_thetas + mesh
```

**模型能力**：
- HMR2 输入 256×256 人体裁剪图（192:256 宽高比），输出 SMPL 24 关节 + 6890 顶点 mesh
- 训练集以直立/日常动作为主，**地面训练（四点支撑/俯卧/仰卧）是弱项**——腕背伸、趾屈等承重角度回归偏差大（鸟狗式实测腕弯折 177° vs 应有 90°）
- YOLOv8n-pose 检测 COCO-17 点（鼻/眼/耳/肩/肘/腕/髋/膝/踝），**无手指/脚趾关键点**——肢端精度依赖腕/踝位置外推
- 2D 锚定置信度门 `KP_CONF_MIN=0.6`，低于此值的关节不参与校正（退化为 HMR2 原值）

**已知弱项（拍摄可弥补）**：
| 问题 | 根因 | 拍摄对策 |
|---|---|---|
| 腕背伸缺失（撑地手垂直指地） | HMR2 训练集缺承重姿态 | 侧位拍摄，确保手腕不被躯干遮挡 |
| 肢端 2D 残差偏高（arm p50~42px） | SMPL 可达流形几何约束 | 穿着贴身长袖，避免宽袖口遮挡手腕 |
| 横卧人体旋转方向选错 | 画面旋转→3D 逆旋转链路复杂 | 尽量竖构图拍摄，头朝上 |
| 多人/道具干扰 YOLO 检测 | YOLOv8n 取 bbox 最大者 | 画面中只出现一人，移除训练器械 |

---

## 2. 环境设置

### 2.1 场地

- **地垫纯色**：使用纯灰/纯深蓝瑜伽垫，避免花纹/网格/文字——YOLO 会将纹理误判为人体边缘
- **背景简洁**：人体背后 1.5-2m 内无杂物、无镜子、无其他人体图片/海报
- **地面平整**：单色硬质地板或地垫，避免地毯毛绒（影响脚踝检测置信度）
- **训练器械**：哑铃/弹力带等器械在拍摄区域外。器械进入画面会干扰 YOLO 人体检测框

### 2.2 光照

- **均匀漫射光**：正面+侧面双光源（如窗户+补光灯），避免单侧硬光造成半身阴影
- **避免逆光**：人体不可背对窗户/强光源——逆光使 YOLO 检测置信度骤降、HMR2 肤色/纹理特征丢失
- **色温稳定**：单一光源色温（日光 5600K 或暖光 3200K），混合色温不影响几何但影响 HMR2 图像特征
- **照度充足**：≥300 lux（普通室内日光灯即可），暗光导致视频噪点 → HMR2 回归退化

### 2.3 无关人员

- 画面中**只允许出现一名受试者**
- 教练/助手站在镜头后方，不可进入画面——YOLO 取 bbox 最大者，但第二人会干扰检测框、可能触发交叉肢体误判

---

## 3. 机位设置

### 3.1 构图（最关键）

```
推荐：侧身 45° 半侧位

           相机
            ◉
             \
              \ 3-4m
               \
    ┌───────────┐
    │           │
    │   受试者   │  ← 面朝画面右侧或左侧
    │   (地垫)   │
    └───────────┘
```

- **机位高度**：镜头中心与受试者腰部齐平（约 0.8-1.0m），三脚架固定
- **拍摄距离**：3-4m，确保全身入画且四周留 10-15% 边距（YOLO bbox 需完整）
- **角度选择**：
  - **半侧位 45°（首选）**：能看到四肢前后运动且无遮挡，适合鸟狗式/死虫式/桥式等地面训练
  - **正侧位 90°**：矢状面运动（前后向）最佳，但左右肢体重叠时 YOLO 可能左右混淆
  - **正面 0°**：适合冠状面运动（左右向），但深度方向运动（前后）3D 还原差
  - **避免俯拍**：顶视角使 HMR2 的直立先验完全失效，旋转预处理也无法补偿

### 3.2 画面方向（竖屏/横屏判定规则）

**核心规则：以脊椎方向为准——脊椎平行于地面时横屏拍，脊椎垂直于地面时竖屏拍。**

判定目的是让画面中人体始终竖立（头朝上、脚朝下），使 HMR2 直立先验生效、避免旋转欺骗流程的额外误差和耗时。

| 姿态 | 脊椎方向 | 拍摄方向 | 说明 |
|---|---|---|---|
| 站立/坐姿/跪姿/蹲姿 | 垂直于地面 | **竖屏** | 人体天然竖立，直接拍 |
| 仰卧/俯卧/桥式 | 平行于地面 | **横屏**（相机旋转 90°） | 让画面中人体竖立，地面变侧墙但 HMR2 先验生效 |
| 鸟狗式/四点支撑 | 接近平行 | **横屏** | 躯干接近水平，旋转后更保险 |
| 侧卧/侧 plank | 45° 临界 | **看实际构图**：bbox 宽≥高则横屏，高>宽则竖屏 | 临界情况以画面中人体外接矩形宽高比为准 |

> **为什么重要**：系统检测到 bbox 宽≥高时自动旋转 90° 推理再逆旋转回原始坐标系（[kineto_core.py:571-589](file:///Users/panbo/Projects/Kineto/kineto-engine/kineto_core.py#L571-L589)），两个方向都推理后选质量分高者——额外耗时 ×2、且翻身/侧撑转换帧可能选错方向。直接横屏拍让人体在画面中竖立即可完全避免此流程。

> **可接受折中**：正常竖屏拍摄横卧人体，旋转欺骗兜底——quality 仍可达 0.99+（实测死虫式 0.9936、鸟狗式 0.9941），但翻身转换帧有选错旋转方向的风险。追求最优精度时建议横屏。

### 3.3 分辨率与帧率

| 参数 | 要求 | 原因 |
|---|---|---|
| 分辨率 | ≥1280×720（推荐 1920×1080） | HMR2 输入 256×256，bbox 裁剪后缩放；原图分辨率越高，小关节（腕/踝）像素证据越充分 |
| 帧率 | ≥25fps（推荐 30fps） | 时序平滑窗口 5 帧，低帧率导致运动突变检测假阳性 |
| 码率 | ≥4 Mbps（1080p） | 过低码率产生块效应，YOLO 关键点检测置信度下降 |
| 格式 | MP4 (H.264) | 管线用 OpenCV 解码，H.264 兼容性最佳 |

### 3.4 相机固定

- **必须使用三脚架**——手持拍摄的抖动会注入 cam_t 逐帧跳变，2D 锚定的射线球交解深度噪声放大
- **禁止变焦/移动机位**——焦距 `focal=5000/256*max(w,h)` 在单次推理中按首帧图像尺寸计算，变焦破坏比例一致性
- **禁用防抖算法**（电子防抖会裁剪/变形画面，OIS 可以保留）——光学防抖不影响，但电子防抖的逐帧裁剪使 bbox 偏移

---

## 4. 人员穿着

### 4.1 服装（直接影响 YOLO 关键点检测率）

| 部位 | 要求 | 原因 |
|---|---|---|
| 上衣 | **贴身长袖或短袖**，纯色（黑/灰/深蓝） | 宽松袖口遮挡手腕，YOLO 腕点置信度 <0.6 时锚定不生效（退化为 HMR2 原值） |
| 下装 | **贴身长裤或短裤**，纯色 | 宽裤腿遮挡脚踝，同上 |
| 鞋袜 | **赤脚或薄底袜**，禁止鞋类 | 鞋子使脚踝检测偏移 +10-15cm，SMPL 脚关节位置失真 |
| 颜色 | 与背景/地垫**高对比度** | 纯色背景+对比色服装使 YOLO 边缘检测最稳定 |
| 图案 | **禁止花纹/字母/条纹** | 花纹干扰 YOLO 人体边缘提取，条纹产生摩尔纹 |

### 4.2 发型与饰物

- **长发扎起**：散发遮挡肩/颈 → YOLO 肩点置信度下降 → 上肢链锚定全链失效（肩是 arm 链的根点，`ROOT_CONF_MIN=0.5`）
- **摘除首饰**：手表/手链在腕部产生局部高对比度斑点，可能被 YOLO 误判为腕点
- **不戴帽子**：帽檐遮挡眼/耳，影响 HMR2 头部朝向估计（头部 thetas 影响颈椎链）

### 4.3 体型建议

- HMR2 的 `betas`（10 维体型参数）从图像回归，贴身衣物使体型估计更准 → betas 影响 SMPL rest 骨长 → 影响 mesh↔joints 一致性（G14 门禁）
- 无体型限制，但极度宽松的衣物等效于改变表观体型 → betas 偏差 → 骨长界失配

---

## 5. 动作执行要求

### 5.1 通用要求

- **动作幅度完整**：每个动作做到终端位（如鸟狗式手脚完全伸直），HMR2 对极端关节角度回归弱，但终端位静止帧的 YOLO 检测最稳定（速度低 → 锚定静止门通过率高）
- **节奏稳定**：2-3 秒/次重复，过快运动使帧间位移 >5cm → 时序平滑检测为突变 → 可能触发不必要的骨长约束
- **面向固定**：一次拍摄中不转身换方向——转身时左右肢体在画面中互换，触发 `_COCO_PARTNER` 交叉假设，增加误判概率

### 5.2 地面训练专项（鸟狗式/死虫式/桥式/俯卧撑）

这些姿态是 HMR2 的已知弱项，拍摄要求更严格：

- **必须竖拍**（相机旋转 90° 安装）：让画面中人体"竖立"，避免旋转欺骗流程的额外误差
- **半侧位 45°**：正面拍四点支撑时手腕被躯干遮挡——侧位可露出支撑手腕
- **地垫颜色与服装高对比**：俯卧/仰卧时大面积躯干贴地，躯干边缘检测依赖服装-地垫色差
- **动作分解慢做**：地面训练的关节角度变化幅度大（腕从悬空到撑地），慢速动作给 HMR2 更多静止帧回归机会

### 5.3 需要避免的动作/画面

| 场景 | 问题 | 建议 |
|---|---|---|
| 多人同框 | YOLO 取 bbox 最大者，可能选错人 | 逐一拍摄 |
| 器械遮挡肢体 | 哑铃遮挡手腕/前臂 | 使用弹力带替代，或空手示范 |
| 肢体交叉（如双手胸前交叉） | 触发 `_COCO_PARTNER` 左右互换假设，可能选错 | 避免交叉动作，或从交叉态缓慢分开 |
| 快速翻滚/转体 | 旋转方向选择失败 + 帧间 cam_t 跳变 | 分段拍摄，每段一个方向 |
| 局部出画（手脚超出画面边缘） | YOLO bbox 不完整 → HMR2 裁剪框偏移 | 增大拍摄距离/广角 |

---

## 6. 文件交付

| 参数 | 要求 |
|---|---|
| 格式 | MP4 (H.264 编码) |
| 时长 | 单动作 10-30 秒（≤885 帧为佳，推理时间 ~1:1） |
| 大小 | ≤100MB（公网传输 via scp 到 `/srv/kineto/inbox/`） |
| 命名 | `{动作名}_{日期}.mp4`，如 `birddog_20260911.mp4` |

上传路径（设备端）：
```bash
scp 视频文件 juxin@aibox:/tmp/ && ssh juxin@aibox 'sudo mv /tmp/视频 /srv/kineto/inbox/'
```

---

## 7. 算法侧优化建议（假设输入遵循上述规范）

> 以下建议基于"输入视频基本符合拍摄规范"的前提，按优先级排列。

### 7.1 地面训练姿态专项修正（P2.2，高优先级）

**问题**：鸟狗式实测支撑手腕弯折 177°（应 90°），SMPL 局部旋转仅 14.8°——HMR2 训练集缺承重姿态。

**可做**：
- 在 Phase3 thetas 合并环节（[kineto_core.py:1866-1884](file:///Users/panbo/Projects/Kineto/kineto-engine/kineto_core.py#L1866-L1884)）之后，增加**地面触地后处理**：
  - 地面估计：clip 级 `ground_y = percentile(Jc[:, GROUND_JOINTS, 1], 90)`，按姿态 regime 切换（站立用下肢、卧姿用核心段/全身包络）
  - 触地检测：**叶点+腕联合口径**（四点支撑时腕离地约一掌厚，单测腕漏检 70%），近地且静止（速度 <0.15m/s），连续 ≥5 帧成段
  - 修正目标：thetas 层（关节 20/21 腕背伸），不是叶点位置——叶点 thetas 在代码中冻结为 HMR2 原值
  - 修正方式：以 HMR2 thetas 为先验，在腕背伸自由度上叠加约束角度（如 +80°），**不替换关节位置**
  - 改后重新 SMPL forward（mesh↔joints 同源），离线渲染目视确认形体

**不可做**：
- ❌ 直接覆写 joints_3d 叶点位置（P2.1 教训：G14 自洽但形体扭曲）
- ❌ 用单帧 max(y) 定义地面（诊断证实偏差 +0.4m）
- ❌ 无静止门控（摆动相经过近地区域会被误判触地）

### 7.2 HMR2 输入分辨率提升（中优先级）

**现状**：HMR2 输入固定 256×256，`image_size=256`，`focal_length=5000.0`（[kineto_core.py:329-330](file:///Users/panbo/Projects/Kineto/kineto-engine/kineto_core.py#L329-L330)）。

**可探索**：
- 4DHumans 的 HMR2 架构（ViT-H 旋转回归器）原生支持更高输入分辨率，但官方权重在 256 上训练。直接提升到 384/512 需要重训或至少微调，否则旋转回归精度下降
- **折中**：保持 256 输入，但改善 bbox 裁剪质量——当前 YOLOv8n 检测框偏松（包含过多背景），可尝试 YOLOv8s-pose（精度更高、仍可实时）或调整 bbox 扩展系数

### 7.3 2D 锚定置信度门自适应（中优先级）

**现状**：`KP_CONF_MIN=0.6` 硬编码（[limb_anchor.py:101](file:///Users/panbo/Projects/Kineto/kineto-engine/limb_anchor.py#L101)），低于 0.6 的 YOLO 关键点不参与锚定。

**可做**：
- 当输入遵循拍摄规范（光照充足、对比度高）时，YOLO 整体置信度分布偏高，可将门限提到 0.65-0.7 减少低质量锚定噪声
- 反之，暗光/遮挡场景自适应降到 0.5，宁可多锚定（后述骨长约束会修正）也不放弃
- 实现：clip 级取 COCO-17 全帧中位置信度作为自适应基准

### 7.4 多机位融合预留（远期）

**现状**：单目 2D→3D 深度不可观（P2.1 分析：射线球交有两解，twist 不可观测）。

**如果未来支持双机位**：
- 第二机位放在正交方向（主侧位 90° + 副正面 0°），两个视角的 2D 射线交点直接确定 3D 深度——消除射线球交的二义性
- 数据结构已预留 `timestamp_ms`（§4 规范），多机位帧对齐可行
- 当前代码无需大改：`anchor_limbs_to_2d` 接受单组 kp2d，可扩展为多组 kp2d 联合求解（射线束最小二乘）

### 7.5 拍摄规范门禁（低优先级，易做）

**可做**：在 POST `/jobs` 前增加预检：
- 检测画面中人数 >1 → 警告
- 检测人体 bbox 占画面 <30% → 警告（距离太远）
- 检测 bbox 宽≥高且持续全片 → 提示"横卧视频，建议竖拍"
- 检测平均亮度 <100 lux → 提示"光照不足"

这些检查在 API 层做、不碰推理管线，可显著减少低质量输入。

### 7.6 旋转欺骗优化（低优先级）

**现状**：横卧视频触发双方向推理（CCW + CW），每帧耗时 ×2。

**如果规范要求竖拍**：
- 横卧触发率降到接近 0，可去掉双方向推理逻辑或降为单方向（节省 50% 推理时间）
- 但需保留兜底：万一用户上传横拍视频，仍需旋转欺骗——可作为"兼容模式"保留

---

## 附录 A：拍摄规范检查清单

```
□ 纯色地垫（灰/深蓝），背景 1.5m 内无杂物
□ 均匀光照 ≥300 lux，无逆光
□ 画面中只有一名受试者
□ 机位：半侧位 45°，高度与腰部齐平，距离 3-4m
□ 竖构图拍摄（头朝上），三脚架固定
□ 分辨率 ≥1280×720，帧率 ≥25fps，H.264 编码
□ 受试者：贴身纯色衣物，赤脚/薄袜，长发扎起，无饰物
□ 服装与地垫高对比度
□ 动作节奏 2-3 秒/次，面向固定不转身
□ 全身入画，四周留 10-15% 边距
□ 单段 10-30 秒，文件 ≤100MB
```

## 附录 B：管线参数速查

| 参数 | 值 | 代码位置 | 含义 |
|---|---|---|---|
| `focal_length` | 5000.0 | kineto_core.py:329 | HMR2 模型空间焦距 |
| `image_size` | 256 | kineto_core.py:330 | HMR2 输入分辨率 |
| `KP_CONF_MIN` | 0.6 | limb_anchor.py:101 | YOLO 关键点锚定门限 |
| `ROOT_CONF_MIN` | 0.5 | limb_anchor.py:136 | 根点（肩/髋）置信度门限 |
| `ERR_MIN_PX` | 8.0 | limb_anchor.py:102 | 重投影误差已对齐阈值 |
| `ERR_MAX_PX` | 700.0 | limb_anchor.py:103 | 重投影误差硬上界 |
| `BLEND_HIT` | 0.85 | limb_anchor.py:125 | 射线命中时混合权重 |
| `BLEND_MISS` | 0.60 | limb_anchor.py:126 | 射线未命中时混合权重 |
| `REFINE_CHANGE_EPS` | 5e-3 | kineto_core.py:991 | 帧改动检测阈值（米） |
| `_BONE_LO_RATIO` | 0.75 | skeleton_spec.py:310 | 骨长下界比例 |
| `_BONE_HI_RATIO` | 1.35 | skeleton_spec.py:310 | 骨长上界比例 |
| BBOX 比例 | 192:256 | kineto_core.py:443 | HMR2 裁剪框宽高比 |
