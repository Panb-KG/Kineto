# 运动康复动作生成与解析 Web 应用 - 敏捷开发计划

> 本计划采用“云边协同”与“模块化解耦”架构，适配一人公司产品开发模式中的极简开发与低运维需求。文档已整理为标准 Markdown 格式，便于直接复制到 Google Docs、Notion 或项目文档中。

## 1. 系统架构蓝图

| 模块 | 部署环境 | 核心技术栈 | 核心任务 |
| --- | --- | --- | --- |
| 视觉算法引擎 | 本地 Intel AI BOX（Arc Pro B60 24GB） | Python、4DHumans、ComfyUI API | 承担重度 GPU 算力：将真人教学视频逆向解析为 3D 姿态 JSON，并运行 FLUX.1 生成 2D 标准四宫格图。 |
| 业务交互大脑 | 云端 Zeabur 服务器（2C 4G） | Next.js、React Three Fiber、Node.js | 承担产品前端呈现：用户界面、3D 骨架可拖拽互动渲染、动作处方库管理。 |
| 云边通讯桥梁 | 局域网 / 公网穿透 | FastAPI、Cloudflare Tunnel（或 FRP） | 将本地 AI BOX 的算力封装为 API，供 Zeabur 云端 Web 应用调用。 |

## 2. 阶段开发路径与 Vibe Coding 提示词

### 阶段一：攻克本地“黑盒”（算法域打通）

目标：在 AI BOX 上实现从“输入视频”到“输出动作 JSON 与标准渲染图”的全自动流水线，不涉及 Web 前端，纯依靠 Trae/Cursor 编写 Python 脚本。

#### 任务 1.1：开发 3D 姿态解析提取器

动作：提取运动视频（如下蹲跳 CMJ、跌落跳 Drop Jump 等）的运动学参数，输出为结构化数据。

Vibe Coding 提示词：

```text
我正在开发一个运动康复辅助系统。请帮我写一个基于 Python 的 3D 人体姿态提取脚本。

请使用开源 3D 姿态模型 4DHumans（或 MMPose 对应的高精度人体模型），并列出完整的 requirements.txt 及安装步骤。

脚本需使用 OpenCV 读取本地的 input_video.mp4，逐帧提取肩、髋、膝、踝等核心关节的三维坐标和旋转角度（SMPL 格式）。

提供一个极简的本地可视化 UI（基于 Tkinter 或 Gradio），允许我拖动视频进度条，手动截取 4 个康复关键帧，并将这 4 帧的 3D 参数保存为 keyframes_4.json。

注意：AI BOX 显存为 24GB，请在推理时确保显存的合理释放。
```

#### 任务 1.2：开发 ComfyUI API 渲染触发器

动作：将解析出的骨骼数据灌入 FLUX.1 管线，生成高度一致的标准化 2D 教学图。

Vibe Coding 提示词：

```text
我有一个导出的 ComfyUI 工作流 API 文件 comfy_api_template.json（其中包含了 FLUX 和 ControlNet 节点）。请帮我写一个 Python 脚本：

读取上一步生成的 keyframes_4.json。

将其中的 4 组 3D 关节参数（Thetas），动态替换到 comfy_api_template.json 中对应的节点参数位。

使用 requests 库向本地 http://127.0.0.1:8188 提交生图任务。

使用 WebSocket 监听生图进度，任务完成后，将返回的超宽长图（4096x1024）自动切割为 4 张 1024x1024 的关键帧图片并保存在本地目录下。
```

### 阶段二：构建前端业务大脑（交互域打通）

目标：在 Zeabur 上部署面向终端客户与大一学生的 Web UI，实现动作处方的可视化交付。

#### 任务 2.1：开发 Web 3D 交互页面

动作：构建 Next.js 前端，三维视角能极其直观地辅助《肌骨解剖触诊》中强调的肩胛运动节律与骨盆中立位展示，避免 AI 视频生成的解剖结构幻觉。

Vibe Coding 提示词：

```text
我正在用 Next.js 开发一个康复动作展示 Web 应用。请帮我编写一个包含以下功能的页面：

页面上半部分：标准的 React 响应式网格，展示 4 张静态的关键帧动作指导图（暂时使用本地占位图）。

页面下半部分：使用 React Three Fiber (R3F) 加载一个基础的 GLTF 3D 人偶模型。

核心功能：写一个组件读取本地的 keyframes_4.json 数据，利用 Tween.js 在 4 个关键姿态间平滑循环播放动作。

开启 OrbitControls，允许用户通过鼠标拖拽旋转、缩放 3D 视角，以便从不同解剖平面观察动作细节。页面整体风格需保持极简和医疗专业感。
```

### 阶段三：云边集成与闭环（工程域打通）

目标：将 Zeabur 上的前端与 AI BOX 上的后端连接，形成完整的自动化产品系统。

#### 任务 3.1：封装本地 API 与网络穿透

动作：利用 FastAPI 将第一阶段的 Python 脚本包装为 Web 接口，以便云端随时调用。

Vibe Coding 提示词：

```text
请帮我用 FastAPI 编写一个轻量级的后端服务：

设立一个 POST /analyze_video 接口，接收前端上传的 mp4 文件。

收到文件后，在后台异步调用我之前写好的 3D 姿态提取脚本和 ComfyUI 渲染脚本。

设立一个 GET /task_status/{task_id} 接口，供前端轮询查询处理进度。

任务完成后，返回包含 4 张 2D 图片 URL 和 1 个 3D 骨架 JSON 数据的统一响应体。

跨域 (CORS) 配置需允许我的 Zeabur 线上域名访问。
```

## 3. 科研级扩展与多模态融合架构（Research & Fusion Layer）

为了确保系统具备向“科研级生物力学平台”演进的能力，并在未来无缝接入测力台（Force Plate）、肌电图（EMG）等外部硬件，系统在底层数据结构与架构设计上预留以下三大能力：

### 3.1 异构数据时间戳对齐机制（Time-Series Synchronization）

- 统一时间基准（Global Clock）：视频数据（通常 30/60/120 Hz）与测力台数据（通常 1000 Hz 以上）存在巨大的采样率差异。系统在解析视频时，将放弃简单的“帧号索引”，强制采用绝对时间戳（Unix Timestamp）或相对毫秒数（Relative ms）作为数据结构的主键。
- 信号同步锚点（Sync Anchors）：在前端与后端的解析逻辑中，预留“同步特征点”标定功能。例如，允许研究人员手动或通过算法标记“脚尖离地（Take-off）”或“初始触地（Initial Contact）”的瞬间，以此作为时间轴的绝对零点，将视频的三维运动学数据与测力台的动力学数据（$F_z$ 曲线）在时间轴上严密缝合。

### 3.2 全量原始数据保留策略（Raw Data Retention）

- 防数据降维：典型的 2C 应用为了节省存储，通常只保留最终渲染的 4 张图片和简化后的 JSON。科研级应用必须遵从“原始数据不可变”原则。
- 持久化机制：系统后台（AI BOX）将完整保留原始高帧率视频文件，以及未经平滑滤波的每一帧 3D 姿态完整参数（包含全身 24+ 关节的旋转矩阵与置信度得分）。这些沉淀的数据将成为未来建立“特定康复人群生物力学常模数据库”的核心资产。

### 3.3 算法解耦与标准格式导出（Algorithm Decoupling & Export）

- 插件化预测器：目前采用 4DHumans 进行姿态估计，但科研领域算法迭代极快（如未来的无标记点动作捕捉技术 markerless mocap）。底层的 Python 架构将采用“工厂模式”封装，确保未来只需替换核心推理代码，而不需要重写上层的 API 和时间戳同步逻辑。
- 科研格式支持：在目前的 Web JSON 交付之外，预留导出为 C3D 格式或标准 CSV 格式的接口，确保数据可以直接导入 OpenSim 或 Visual3D 等专业生物力学软件进行更深度的逆向动力学计算。

## 4. 补充 Vibe Coding 提示词（面向科研预留）

在进行第一阶段“视频姿态提取”的 AI 辅助编程时，使用以下改进版的提示词，可以在不增加过多开发工作量的前提下，直接把科研所需的底层数据结构打好底子：

### 修改版 1.1 提示词（强化时间戳与高频数据预留）

```text
我正在开发一个科研级的运动康复姿态提取 Python 脚本，采用 4DHumans（或同级别高精度模型）。请在编写代码时严格遵循以下数据约束：

读取 input_video.mp4 时，必须利用 OpenCV 提取视频的真实 FPS 和时长信息。

在输出的 pose_data.json 中，不能仅仅使用 frame_index。每个数据点必须包含精确的 timestamp_ms（相对视频开头的毫秒数），以方便未来与 1000Hz 的测力台 CSV 数据进行时间序列对齐。

JSON 数据结构需要采用类似这样的嵌套：
{
  "metadata": {
    "video_fps": 60,
    "total_frames": 180,
    "resolution": "1080p"
  },
  "frames": [
    {
      "timestamp_ms": 16.67,
      "frame_index": 1,
      "smpl_thetas": [...],
      "confidence_score": 0.95
    }
  ]
}

请将底层的模型推理逻辑封装成一个独立的 class PoseEstimator，做到高内聚低耦合，方便我未来随时替换为其他更先进的 SOTA 视觉大模型。
```

## 5. 总结

该方案的核心思想是：

1. 在本地 AI BOX 上先完成算法闭环，确保数据质量和模型可控；
2. 在云端 Zeabur 上完成产品展示与交互体验；
3. 通过 FastAPI 与网络穿透技术打通云边协同；
4. 在底层结构中预留科研扩展能力，便于未来接入测力台、EMG 等硬件与专业生物力学工具链。

这一架构既适合快速启动 MVP，也能够自然演进为科研级运动康复平台。
