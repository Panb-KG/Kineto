# =============================================================================
# Kineto / deploy — .env.example   (docker compose 变量注入模板)
# -----------------------------------------------------------------------------
# 用法（在设备上）:
#     cd /opt/kineto/deploy
#     cp .env.example .env
#     nano .env                 # 填下面两个必填项
#     chmod 600 .env            # ★ 里面有密钥，别留 644
#     docker compose config     # 先干跑一次，确认变量被正确注入
#     docker compose up -d engine
#
# 说明:
#   · docker-compose.yml 对这两项用了 ${VAR:?...} **强校验**：缺值时 compose 直接
#     报错退出，不会带着空密钥启动（api.py 现为 fail-closed：KINETO_API_KEY 未设且
#     未设 KINETO_ALLOW_NO_AUTH=1 → 受保护端点直接 503，不再 WARN+放行）。
#   · 本文件只是模板，可以进版本库；真正的 `.env` **不要**提交。
#   · 下面除两个必填项外，还有 **质量门禁**（KINETO_QUALITY_GATE / KINETO_QUALITY_THRESHOLD）
#     与 **性能/缓存/长视频天花板** 一组可选 env（默认值即引擎内置默认，不填=当前行为）。
#   · systemd 主路径不用这个文件，它用 /etc/kineto/kineto-engine.env
#     （见 deploy/kineto-engine.service 的 EnvironmentFile=- 与 DEPLOY_MOFANG.md §5.8）。
#     两条路径请**共用同一把 key**，这样 Zeabur 前端的 X-API-Key 只需配一次。
#     质量/性能类 env 在 systemd 路径上写进同一个 /etc/kineto/kineto-engine.env 即可生效。
# =============================================================================

# -----------------------------------------------------------------------------
# 必填 1：API 鉴权密钥（请求头 X-API-Key）
#   生成一把强随机的：
#     openssl rand -hex 32
#   同一把值还要写到 Zeabur 前端的【服务端】环境变量 KINETO_API_KEY（无 NEXT_PUBLIC_
#   前缀，由 app/api/[...path]/route.ts 在服务端注入 X-API-Key，见 DEPLOY_ZEABUR.md）。
#   ⚠️ 切勿写成 NEXT_PUBLIC_API_KEY —— 那会把密钥内联进浏览器包泄露。
# -----------------------------------------------------------------------------
KINETO_API_KEY=

# -----------------------------------------------------------------------------
# CORS 源（前端地址），逗号分隔可写多个——【纵深防御】
#   新代理架构下浏览器【不再直连引擎】（只与前端同源 /api/* 通信），功能上已不依赖 CORS；
#   但 docker-compose.yml 与 kineto-engine.service 的启动守卫仍会要求填一个值（纵深防御），
#   照填前端精确 origin 即可；仅当仍保留“浏览器直连引擎”的老路径时，它才真正生效。
#   例：KINETO_CORS_ORIGINS=https://kineto.<你的域名>
#   注意：必须带 scheme、不带结尾斜杠，否则（老直连路径的）浏览器预检会失败。
# -----------------------------------------------------------------------------
KINETO_CORS_ORIGINS=

# -----------------------------------------------------------------------------
# 生产严格模式（推荐恒为 1）—— ★ 语义已收敛（引擎 P3/改动 F），务必读清楚：
#   KINETO_STRICT=1 现在**只**守「假数据 / 缺权重 / detector 降级」的 fail-fast：
#     · 4DHumans 权重加载失败        → 拒绝回退到内置简化模型
#     · YOLOv8n 人体检测器加载失败   → 拒绝静默降级为全图默认框
#     · 简化回退管线（合成关键点 + 全零 thetas）→ 拒绝写出假数据
#   触发时 kineto_core.py 抛 StrictModeRefused、以**退出码 3** 结束（api.py 把该 job 判 failed）。
#   ❗ STRICT **不再对质量做门禁**（已与质量彻底解耦）：它绝不会因「质量分低 / 审计
#      verdict=warn|fail」而让 job 失败。质量门禁改由下面的 KINETO_QUALITY_GATE 独立控制。
#      （旧版把 STRICT 当质量门禁用，导致 deploy 烘了 STRICT=1 时 done→failed 大面积回归，已修。）
#   docker-compose.yml 默认 ${KINETO_STRICT:-1}，即不填也是 1；仅本机调试想允许回退时才置 0（生产禁止）。
# -----------------------------------------------------------------------------
KINETO_STRICT=1

# -----------------------------------------------------------------------------
# ★ 质量门禁（取代旧版“STRICT 兼做质量门禁”的做法；与 KINETO_STRICT 完全解耦）
#   KINETO_QUALITY_GATE = off | warn | fail（**默认 warn，即本文件的值，安全**）
#     off  → 从不门禁：永远 done，且不加质量告警
#     warn → （默认）质量不达标时**仍 done**，但附 degraded=true / quality_warning=true
#            显式暴露给前端与运维（绝不静默放行，也绝不 mass-fail）
#     fail → 不达标才判 failed（failed 分支不带 quality_score，守前端 JobStatus 契约）
#   “不达标” = audit_results.json 的 verdict ∈ {warn,fail}（主信号）
#              或 final_quality_score < KINETO_QUALITY_THRESHOLD（辅信号）。
#   ⚠️ 生产建议保持 warn：既不会因质量把可用结果判死，也不会把问题藏起来。
#      只有在“宁可不交付也不要降级结果”的场景才切 fail。
# -----------------------------------------------------------------------------
KINETO_QUALITY_GATE=warn

# 质量分阈值（辅助信号；引擎默认同为 0.6）。P1/P2 整改后干净数据的诚实分实测 ~0.99，
# 远高于 0.6，故该值不会误判好数据；重标定请依据 quality_log.jsonl 的累积记录离线做。
# deploy/validate.sh 的 G6 也用它当验收阈值（--min-quality > KINETO_MIN_QUALITY > 本变量 > 0.6）。
KINETO_QUALITY_THRESHOLD=0.6

# -----------------------------------------------------------------------------
# 可选：性能 / 缓存 / 长视频天花板（引擎 P4/改动 G）
#   ★ 下面每一项的注释值 **就是引擎内置默认**：不填（保持注释）= 当前行为，零风险。
#   ★ 生效路径（两条都支持，任选其一）：
#       · docker compose 路径 → 在本文件取消注释即可（docker-compose.yml 用 ${VAR:-默认} 透传）
#       · systemd 路径        → 写进 /etc/kineto/kineto-engine.env（unit 的 EnvironmentFile=- 读取，
#                               会覆盖 unit 里的 Environment= 同名值），然后 systemctl restart kineto-engine
# -----------------------------------------------------------------------------
# 审计问题帧可视化（audit_iter*/audit_frame_*.jpg）。默认开=保持当前行为；
# 设 0/false/no/off 关闭可省 seek/IO。可视化在评分之后运行且不写回结果，**不改 score/verdict**。
#KINETO_AUDIT_VISUALIZE=1
# 审计采样帧缓存容量（有界 FIFO，跨 iteration/跨 check 复用已解码帧，消除最多 ~90 次冗余 seek）。
# 只省 IO、**不改被审计帧集合**；<=0 禁用复用（每次重新解码）。长视频不建议调太大（占内存）。
#KINETO_AUDIT_CACHE_FRAMES=48
# 显存释放节流步长：每 N 帧调一次 clear_device_memory（原为逐帧）。只调 empty_cache()，
# **不改任何计算结果**（joints/thetas/score 逐位一致），仅降低释放开销；=1 恢复逐帧原行为。
#KINETO_CLEAR_MEM_EVERY=30
# 架构级“支持帧数上限”声明：超过该帧数时引擎在 stderr 响亮告警（不阻断）——
# pose_data.json/mp4 随帧数线性膨胀（~9.16KB/帧），前端全量 fetch+parse 可能卡顿。
# 长视频请分段处理；分块/流式交付与帧率降采样属未来缓解项。
#KINETO_SUPPORTED_FRAME_CEILING=9000
# quality_log.jsonl 轮转上限（字节）。超过即轮转为 .jsonl.1（只留一份备份）；
# best-effort，绝不影响 job 结局。<=0 关闭轮转（回到单文件无限增长）。
# ⚠️ 这一项**不要传空值**：引擎对空值回退到 0（=关闭轮转），与默认 5MB 不同。
#KINETO_QUALITY_LOG_MAX_BYTES=5242880
# 子进程超时按帧数缩放：timeout = clamp(BASE + 帧数×PER_FRAME, FLOOR, MAX)。
# 短视频命中 FLOOR（与旧版固定 7200s 行为逐位一致）；长视频按帧数线性放宽，避免误超时。
# 帧数由 cv2 探测，探测失败回退 FLOOR。四个值都可单独调。
#KINETO_TIMEOUT_FLOOR_SEC=7200
#KINETO_TIMEOUT_BASE_SEC=1800
#KINETO_TIMEOUT_PER_FRAME_SEC=0.8
#KINETO_TIMEOUT_MAX_SEC=21600

# -----------------------------------------------------------------------------
# 可选：构建期参数（覆盖 docker-compose.yml 里 build.args 的默认值）
# -----------------------------------------------------------------------------
# 宿主是 Ubuntu 22.04 时改 jammy（但 XPU wheel 未在 22.04 验证，见 DEPLOY_MOFANG.md §12 Q2）
#INTEL_GPU_SUITE=noble
# 置 1 则在镜像里装 intel-extension-for-pytorch==2.7.10+xpu（算子融合等加速）
#INSTALL_IPEX=0
